# Use the in-browser demo (no squat-api). Production S3 builds force this on.
VITE_USE_MOCK_API=true

# Set this instead when you want the real Nest API locally:
# VITE_USE_MOCK_API=false
# VITE_API_BASE_URL=http://localhost:3001/api/v1
